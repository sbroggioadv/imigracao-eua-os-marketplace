---
description: Alias de /imigracao — orquestrador imigracao-master (3 frentes, gates de vigência e representação).
argument-hint: [descrição da demanda de imigração EUA]
---

Você foi acionado pelo comando `/imigracao-master` do plugin **imigracao-eua-os** (alias de `/imigracao`).

Argumento recebido: `$ARGUMENTS`

Siga o **mesmo protocolo** de `/imigracao`: acionar a skill **`imigracao-master`**, identificar frente e fase, abrir pelos gates `varredura-de-vigencia` + `trava-de-representacao`, rotear e fechar com Suprema Corte + validador.
